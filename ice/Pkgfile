# Description: A configuration file manager. (git checkout)
# URL:         https://github.com/ix/ice
# Packager:    arcetera, arcetera@openmailbox.org
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports/issues
# Depends on:  git

name=ice
version=git
release=1
source=()

build() {
		cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone https://github.com/ix/ice.git -b master $name
		cd $name
	fi

  mkdir -p $PKG/usr/bin

	./install $PKG/usr/bin
}
