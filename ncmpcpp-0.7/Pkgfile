# Description: An almost exact clone of ncmpc with some new features (git checkout)
# URL:         http://rybczak.net/ncmpcpp
# Packager:    Michael Savage, mike at mikejsavage dot co dot uk
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports/issues
# Depends on:  git ncurses libmpdclient boost taglib

name=ncmpcpp-0.7
version=git
release=1
source=(fix-build.patch)

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://github.com/arybczak/ncmpcpp $name
		cd $name
	fi

	patch < $SRC/fix-build.patch

	./autogen.sh --prefix=/usr --mandir=/usr/man --disable-clock

	make
	make DESTDIR=$PKG install

	rm -rf $PKG/usr/share
}
