# Description: A smart and user-friendly command line shell for OSX, Linux, and the rest of the family. (git checkout)
# URL:         http://fishshell.com
# Packager:    lee, rtlanceroad at gmail dot com
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports-git/issues
# Depends on:  git doxygen libpcre2
# Optional:    python xsel

name=fish
version=git
release=4
source=(fix-help.patch)

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://github.com/fish-shell/fish-shell $name
		cd $name
	fi

	# Fix --help (thanks ryu0 for the patch)
	# pkgmk compresses all man pages, and unpatched fish can't read that
	patch -p1 < $SRC/fix-help.patch

	autoconf
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--without-included-pcre2

	make
	make DESTDIR=$PKG install

	install -d $PKG/bin
	ln -s /usr/bin/fish $PKG/bin

	rm -rf $PKG/usr/share/locale
}
