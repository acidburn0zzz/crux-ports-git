# Description: A smart and user-friendly command line shell for OSX, Linux, and the rest of the family.
# URL:         https://fishshell.com
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports-git/issues
# Depends on:  git doxygen libpcre2
# Optional:    python xsel

name=fish
version=git
release=6
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone https://github.com/$name-shell/$name-shell $name
		cd $name
	fi

	autoreconf --no-recursive
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--without-included-pcre2

	make
	make DESTDIR=$PKG install

	install -d $PKG/bin
	ln -s /usr/bin/fish $PKG/bin

	rm -rf $PKG/usr/share/{locale,doc}
}
