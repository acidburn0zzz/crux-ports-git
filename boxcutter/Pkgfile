# Description: Openbox rc.xml generator (Scheme/Sexpr to XML).
# URL:         https://github.com/nv-vn/boxcutter
# Packager:    arcetera, arc@plusreed.com
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports-git/issues
# Depends on:  git guile openbox

name=boxcutter
version=git
release=1
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://github.com/nv-vn/boxcutter.git -b master $name
		cd $name
	fi

	mkdir -p $PKG/usr/local/bin

	make
	make DESTDIR=$PKG install
}
