# Description: The Steam gaming platform by Valve.
# URL:         http://store.steampowered.com/
# Packager:    Matt Housh, jaeger at morpheus dot net
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports/issues
# Depends on:  xdg-utils mesa3d-32

name=steam
version=1.0.0.49
release=1
source=(http://repo.steampowered.com/steam/archive/precise/steam-launcher_${version}_all.deb)

build() {
	ar p steam-launcher_${version}_all.deb data.tar.gz | tar -C $PKG -zxf -
	mv $PKG/usr/share/man $PKG/usr/
	rm -r $PKG/{etc,usr/share}
	ln -sf /bin/true $PKG/usr/bin/steamdeps
}
