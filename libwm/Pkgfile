# Description: A small library for X windows manipulation.
# URL:         https://github.com/wmutils/libwm
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports-git/issues
# Depends on:  git xorg-libxcb

name=libwm
version=git
release=1
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://github.com/wmutils/$name $name
		cd $name
	fi

	make
	make DESTDIR=$PKG PREFIX=/usr MANPREFIX=/usr/share/man install
}
