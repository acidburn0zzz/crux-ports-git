# Description: Allows easy processing of keyboard entry from terminal-based programs - neovim fork
# URL:         https://github.com/neovim/libtermkey
# Packager:    Luiz de Milon, kori at openmailbox dot org
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports/issues
# Depends on:  git unibilium

name=libtermkey
version=git
release=1
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone https://github.com/neovim/libtermkey $name
		cd $name
	fi

	make
	make PREFIX="/usr" MANDIR="/usr/man" DESTDIR=${PKG} install
}
