# Description: dmenu with XFT, quiet, xy, token, fuzzy matching, followfocus, tabnav, filter and mouse support. (git checkout)
# URL:         https://github.com/toolpunk/dmenu-ee
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports-git/issues
# Depends on:  git xorg-libxft xorg-libxinerama

name=dmenu-ee
version=git
release=1
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://github.com/toolpunk/dmenu-ee
		cd $name
	fi

	make
	make PREFIX=/usr DESTDIR=$PKG install
}
