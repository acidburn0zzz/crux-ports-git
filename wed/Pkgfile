# Description: A terminal text editor with key bindings commonly used in Windows based editors. (git checkout)
# URL:         https://github.com/rgburke/wed
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports-git/issues
# Packager:    onodera, onodera at openmailbox dot org
# Depends on:  git

name=wed
version=1
release=git
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://github.com/rgburke/wed $name
		cd $name
	fi

	make
	make PREFIX=$PKG/usr install
}
