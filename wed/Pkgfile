# Description: A terminal text editor with key bindings commonly used in Windows based editors.
# URL:         https://github.com/rgburke/wed
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports-git/issues
# Depends on:  git source-highlight

name=wed
version=3
release=git
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://github.com/rgburke/$name $name
		cd $name
	fi

	make
	make DESTDIR=$PKG PREFIX=/usr install
}
