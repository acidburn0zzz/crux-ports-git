# Description: A small X utility to perform elementary actions on windows - git checkout 
# URL:         https://github.com/baskerville/xdo
# Packager:    onodera, onodera at openmailbox dot org
# Maintainer:  onodera, onodera at openmailbox dot org
# Depends on:  xorg-libxcb xorg-xcb-util-wm

name=xdo
version=git
release=1
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://github.com/baskerville/xdo $name
		cd $name
	fi

	make
	make DESTDIR=$PKG install
}
