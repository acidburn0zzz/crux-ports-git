# Description: An almost exact clone of ncmpc with some new features
# URL:         http://rybczak.net/ncmpcpp
# Packager:    Michael Savage, mike at mikejsavage dot co dot uk
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports/issues
# Depends on:  ncurses libmpdclient boost taglib

name=ncmpcpp
version=0.6.4
release=1
source=(
	http://ncmpcpp.rybczak.net/stable/ncmpcpp-$version.tar.bz2 \
	fix-build.patch
)

build() {
	cd $name-$version

	patch < ../fix-build.patch

	./autogen.sh --prefix=/usr --mandir=/usr/man --disable-clock

	make
	make DESTDIR=$PKG install

	rm -rf $PKG/usr/share
}
