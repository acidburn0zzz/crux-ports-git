# Description: Just-in-time compiler and drop-in replacement for Lua 5.1 - git checkout
# URL:         http://luajit.org/
# Packager:    Luiz de Milon, kori at openmailbox dot org
# Maintainer:  Luiz de Milon, kori at openmailbox dot org
# Depends on:  git

name=luajit
version=git
release=1
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone http://luajit.org/git/luajit-2.0 $name
		cd $name
	fi

	make amalg
	make DESTDIR=$PKG PREFIX=/usr INSTALL_MAN=$PKG/usr/man/man1 install
}
