# Description: Just-in-time compiler and drop-in replacement for Lua 5.1.
# URL:         http://luajit.org/
# Packager:    Luiz de Milon, kori at openmailbox dot org
# Maintainer:  Luiz de Milon, kori at openmailbox dot org
# Depends on:  git

name=luajit
version=2.0.3
release=1
source=(http://luajit.org/download/LuaJIT-${version}.tar.gz)

build() {
	cd LuaJIT-${version}

	make amalg
	make DESTDIR=${PKG} PREFIX=/usr INSTALL_MAN=$PKG/usr/man/man1 install
}
