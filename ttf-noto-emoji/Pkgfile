# Description: Google Noto emoji fonts. (git checkout)
# URL:         https://www.google.com/get/noto/
# Packager:    onodera, onodera at openmailbox dot org
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports-git/issues
# Depends on:  fontconfig

name=ttf-noto-emoji
version=git
release=1
source=()

build()	{
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://github.com/googlei18n/noto-emoji $name
		cd $name
	fi

	install -Dm644 fonts/NotoEmoji-Regular.ttf $PKG/usr/share/fonts/X11/TTF/Noto-Emoji/Regular.ttf
}
