# Description: A modern and intuitive terminal-based text editor. (git checkout)
# URL:         https://github.com/zyedidia/micro
# Maintainer:  onodera, https://github.com/6c37/crux-ports-git/issues
# Depends on:  go
# Optional:    xclip xsel

name=micro
version=git
release=1
source=()

build() {
	export GOPATH=$(pwd)
	go get -u -v -fix github.com/zyedidia/micro/cmd/micro

	install -Dm755 bin/micro $PKG/usr/bin/micro
}
