# Description: A modern and intuitive terminal-based text editor. (git checkout)
# URL:         https://github.com/zyedidia/micro
# Maintainer:  onodera, https://github.com/6c37/crux-ports-git/issues
# Depends on:  go
# Optional:    xclip xsel

name=micro
version=git
release=1
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://github.com/zyedidia/micro $name
		cd $name
	fi

	export GOPATH=$(pwd)

	sed -i "s/go build/go build -v/" Makefile

	make
	install -Dm755 micro $PKG/usr/bin/micro
}
