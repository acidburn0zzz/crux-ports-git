# Description: A minimal color picker. (git checkout)
# URL:         https://github.com/bwr/colorpick
# Packager:    onodera, onodera at openmailbox dot org
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports-git/issues
# Depends on:  

name=colorpick
version=git
release=1
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://github.com/bwr/colorpick $name
		cd $name
	fi

	make
	install -Dm755 _colorpick $PKG/usr/bin/colorpick
}
