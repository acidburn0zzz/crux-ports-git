# Description: Protocol Buffers - Google's data interchange format. (git checkout)
# URL:         https://developers.google.com/protocol-buffers/
# Packager:    Name and email go here.
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports/issues
# Depends on:  git

name=protobuf
version=git
release=1
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://github.com/google/protobuf -b master $name
		cd $name
	fi

	./autogen.sh
	./configure \
		--prefix=/usr \
		--mandir=/usr/man
	make
	make DESTDIR=$PKG install
}
# vim: set ft=sh noet :
