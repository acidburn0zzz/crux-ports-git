# Description: last.fm/libre.fm scrobbler for mpd - git checkout
# URL:         http://git.musicpd.org/cgit/master/mpdscribble.git/
# Packager:    onodera, onodera at openmailbox dot org
# Maintainer:  onodera, onodera at openmailbox dot org
# Depends on:  git libmpdclient glib

name=mpdscribble
version=git
release=1
source=()

build () {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master 
	else
		git clone git://git.musicpd.org/master/mpdscribble $name
		cd $name
	fi

	./autogen.sh --prefix=/usr --mandir=/usr/man 

	make
	make DESTDIR=$PKG install

	rm -rf $PKG/usr/share
}
