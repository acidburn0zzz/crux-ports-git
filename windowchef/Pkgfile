# Description: A stacking window manager that cooks windows with orders from the Waitron.
# URL:         https://github.com/tudurom/windowchef
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports-git/issues
# Depends on:  git xorg-libxcb xorg-xcb-util-wm

name=windowchef
version=git
release=1
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone https://thetudor.ddns.net/$name $name
		cd $name
	fi

	make
	make DESTDIR=$PKG PREFIX=/usr MANPREFIX=/usr/share/man install
}
