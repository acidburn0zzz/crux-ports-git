# Description: Binary space partitioning window manager. (git checkout)
# URL:         https://github.com/baskerville/bspwm
# Packager:    onodera, onodera at openmailbox dot org
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports/issues
# Depends on:  git xorg-libxcb xorg-xcb-util xorg-xcb-util-wm 

name=bspwm
version=git
release=1
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://github.com/baskerville/bspwm $name
		cd $name
	fi

	make
	make PREFIX=/usr MANPREFIX=/usr/man DESTDIR=$PKG install
}
