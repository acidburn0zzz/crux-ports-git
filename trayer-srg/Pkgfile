# Description: trayer fork with multi monitor support, cleaned up codebase and other fancy stuff. (git checkout)
# URL:         https://github.com/sargon/trayer-srg
# Packager:    rtlanceroad, rtlanceroad at gmail dot com
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports-git/issues
# Depends on:  git gtk

name=trayer-srg
version=git
release=1
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://github.com/sargon/trayer-srg $name
		cd $name
	fi

	./configure \
		--prefix=/usr

	make
	make DESTDIR=$PKG install
}
