# Description: Yet another program to set wallpaper for your X11. (git checkout)
# URL:         https://github.com/arthur-peka/simple-wp-setter
# Packager:    onodera, onodera at openmailbox dot org
# Maintainer:  onodera, onodera at openmailbox dot org
# Depends on:  

name=simple-wp-setter
version=git
release=1
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://github.com/arthur-peka/simple-wp-setter $name
		cd $name
	fi

	make
	install -Dm755 simple-wp-setter $PKG/usr/bin/simple-wp-setter
}
