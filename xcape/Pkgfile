# Description : Linux utility to configure mondifier keys to act as other keys when pressed and released on their own.
# URL         : http://github.com/alols/xcape
# Packager    : wildefyr, wildefur at gmail dot com
# Maintainer  : 6c37 Team, https://github.cm/6c37/crux-ports/issues
# Depends on  : git gcc make xorg-libx11 xorg-libxtst xorg-libxi

name=xcape
version=git-7fca36
sversion=7fca36453eb2ce7c61afa2f1e4571c81c4abcefd
release=2
source=()

gitsource=git://github.com/alols/xcape

build() {
    cd $PKGMK_SOURCE_DIR

    if cd $name; then
        git reset --hard
        git pull $gitsource
    else
        git clone $gitsource $name
        cd $name
    fi

    git checkout $sversion

    make
    make DESTDIR=$PKG PREFIX=/usr MANPREFIX=/usr/man install
}
