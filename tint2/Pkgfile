# Description: A lightweight panel/taskbar for Linux. (git checkout)
# URL:         https://gitlab.com/o9000/tint2
# Packager:    Lains, lainsce at openmailbox dot org
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports/issues
# Depends On:  imlib2 xorg-libxcomposite xorg-libxinerama xorg-libxrandr pango startup-notification

name=tint2
version=git
release=1
source=()

build() {
    cd $PKGMK_SOURCE_DIR

    if cd $name; then
        git fetch -q; git reset --hard origin/master
    else
        git clone https://gitlab.com/o9000/tint2.git $name
        cd $name
    fi

    cmake .
    make
    make DESTDIR=$PKG PREFIX=/usr MANPREFIX=/usr/man install

    rm -rf $PKG/usr/share/tint2
}
