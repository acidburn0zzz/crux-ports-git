# Description: Library for making brushstrokes.
# URL:         https://github.com/mypaint/libmypaint
# Packager:    Lains, lainsce at airmail dot cc
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports-git/issues
# Depends on:  scons json-c python gegl gobject-introspection

name=libmypaint
version=git
release=2
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://github.com/mypaint/libmypaint.git $name
		cd $name
	fi

	scons prefix=/usr enable_gegl=true use_sharedlib=yes \
		--install-sandbox=$PKG $PKG

	rm -rf $PKG/share/locale
}
