# Description: Repository of firmware blobs for use with the Linux kernel.
# URL:         https://git.kernel.org/cgit/linux/kernel/git/firmware/linux-firmware.git/
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports-git/issues
# Depends on:  git

name=linux-firmware-git
version=git
release=1
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://git.kernel.org/pub/scm/linux/kernel/git/firmware/$name $name
		cd $name
	fi

	make DESTDIR=$PKG FIRMWAREDIR=/lib/firmware install

	rm $PKG/lib/firmware/{Makefile,README,configure,GPL-3}
}
